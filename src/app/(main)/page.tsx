import HomeScreen from "@modules/home/screens/Main";

export const metadata = {
  title: `Home - M-Feed`,
  description: `Browse your feed, see your friends posts and your own.`,
};

const Home = () => {
  return <HomeScreen />;
};

export default Home;
